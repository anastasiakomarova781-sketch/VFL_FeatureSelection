# Запуск обучения в Docker контейнере

## 🐳 Вы находитесь в Docker контейнере

Если вы видите `root@...:/app#`, значит вы внутри Docker контейнера.

## ❌ Неправильно (из контейнера):

```bash
cd /Users/akomarova/Documents/GitHub/VFL_FeatureSelection/vfl-master@73591d69c04
# ❌ Этот путь не существует в контейнере!
```

## ✅ Правильно: Вариант 1 - Запуск с хоста

**Выйдите из контейнера** (нажмите `exit` или `Ctrl+D`) и выполните на хосте:

```bash
cd /Users/akomarova/Documents/GitHub/VFL_FeatureSelection/vfl-master@73591d69c04
export PYTHONPATH="../ucbfl_mock:$PYTHONPATH"
bash run_training_local.sh
```

## ✅ Правильно: Вариант 2 - Запуск из контейнера

Если вы хотите запустить обучение **внутри Docker контейнера**, нужно:

### 1. Проверить структуру в контейнере:

```bash
# Внутри контейнера
pwd
ls -la
ls -la vfl-master@73591d69c04/ 2>/dev/null || echo "Папка не смонтирована"
```

### 2. Если папка vfl-master не смонтирована, нужно:

**Вариант A:** Выйти из контейнера и запустить с хоста (рекомендуется)

**Вариант B:** Смонтировать папку vfl-master в docker-compose.yml:

```yaml
volumes:
  - ./vfl-master@73591d69c04:/app/vfl-master:ro
```

### 3. Запуск из контейнера (если папка смонтирована):

```bash
# Внутри контейнера
cd /app/vfl-master@73591d69c04
export PYTHONPATH="/app/ucbfl_mock:$PYTHONPATH"
bash run_training_local.sh
```

## 🔍 Проверка: Где вы находитесь?

```bash
# Проверка 1: Имя хоста
hostname
# Если видите что-то вроде "cfbbf337b467" - вы в контейнере
# Если видите "IT-MAC-NB223" - вы на хосте

# Проверка 2: Путь
pwd
# В контейнере: /app
# На хосте: /Users/akomarova/Documents/GitHub/VFL_FeatureSelection/...

# Проверка 3: Пользователь
whoami
# В контейнере: root
# На хосте: akomarova
```

## 🎯 Рекомендация

**Для обучения VFL лучше запускать с хоста**, так как:
- Нужны серверы на разных портах (50000, 50001)
- Нужен доступ к mock версии ucbfl
- Проще управлять процессами

**Команда для хоста:**
```bash
# Выйдите из контейнера (exit)
cd /Users/akomarova/Documents/GitHub/VFL_FeatureSelection/vfl-master@73591d69c04
export PYTHONPATH="../ucbfl_mock:$PYTHONPATH"
bash run_training_local.sh
```

## 📝 Если нужно запустить в Docker

Обновите `docker-compose.yml`:

```yaml
volumes:
  - ./vfl-master@73591d69c04:/app/vfl-master
  - ./ucbfl_mock:/app/ucbfl_mock
```

Затем пересоздайте контейнер:
```bash
docker-compose down
docker-compose up -d
docker-compose exec vfl-feature-selection bash
```

И внутри контейнера:
```bash
cd /app/vfl-master@73591d69c04
export PYTHONPATH="/app/ucbfl_mock:$PYTHONPATH"
bash run_training_local.sh
```

